{
  "$schema": "https://turbo.build/schema.json",
  "globalPassThroughEnv": ["GOCACHE", "HOME", "LOCALAPPDATA", "XDG_CACHE_HOME"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", ".next/**", "!.next/cache/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "type-check": {
      "dependsOn": ["^type-check"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
