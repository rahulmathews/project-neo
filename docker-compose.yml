services:
  workers:
    build:
      context: ./apps/workers
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    networks:
      - neo-network
    restart: unless-stopped

  graphql-api:
    build:
      context: ./packages/graphql-api
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    networks:
      - neo-network
    restart: unless-stopped

networks:
  neo-network:
    driver: bridge
